<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.NYA_calculation.repository.SlipRepository">

    <insert id="insert"
            parameterType="com.example.NYA_calculation.repository.entity.Slip"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO slips(
        status, step, total_amount, user_id, approver_id
        ) VALUES(
        #{status}, #{step}, #{totalAmount}, #{userId}, #{approverId}
        )
    </insert>
    <select id="findAll" resultType="com.example.NYA_calculation.repository.entity.Slip">
        SELECT
        id,
        status,
        step,
        total_amount,
        user_id,
        approver_id,
        created_date
        FROM slips
        ORDER BY created_date DESC
    </select>
    <select id="findByUserId" parameterType="int" resultType="com.example.NYA_calculation.repository.entity.Slip">
        SELECT *
        FROM slips
        WHERE user_id = #{userId}
        ORDER BY updated_date DESC
    </select>

</mapper>